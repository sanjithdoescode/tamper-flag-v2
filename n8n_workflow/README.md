i have two different workflows.

```gemini_validation.json``` is for the ease of access without having to mess with my slowpoke computer which cannot comprehend LLMs being run locally. 

```llm_validation.json``` remains as the default workflow since that delivers what has been promised 'em-dash' local validation.